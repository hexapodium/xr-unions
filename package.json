{
  "name": "xr-unions",
  "private": true,
  "type": "module",
  "scripts": {
    "cache-airtable": "nvs exec krypton node --env-file=.env scripts/cache-airtable.js",
    "schema": "nvs exec krypton node --env-file=.env scripts/dump-schema.js",
    "debug": "nvs exec krypton node --env-file=.env scripts/cache-airtable.js",
    "test": "nvs exec krypton node --test"
  },
  "dependencies": {
    "airtable": "^0.12.2"
  }
}
